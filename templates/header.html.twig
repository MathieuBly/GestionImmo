<style>
	.navbar {
		background-color: #5daca2;
		height: 60px;
		width: 100%;
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 0 20px;
		position: fixed;
		top: 0;
		z-index: 999;
	}

	.navbar-container {
		width: 100%;
		display: flex;
		justify-content: space-between;
		align-items: center;
	}

	.logo img {
		height: 40px;
	}

	.nav {
		display: flex;
		align-items: center;
	}

	.link {
		color: white;
		text-decoration: none;
		font-weight: 500;
		padding: 8px 12px;
		margin-right: 10px;
		transition: color 0.3s ease;
	}

	.link:hover {
		color: #dfe6e9;
	}

	.separator {
		border-left: 1px solid rgba(255, 255, 255, 0.4);
		height: 25px;
		margin: 0 10px;
	}
</style>

<!-- Navbar avec séparateurs -->
<div class="navbar">
	<div
		class="navbar-container">

		<!-- Logo -->
		<div class="logo">
			<a href="{{ path('app_property_index') }}">
				<img src="{{ asset('theme/assets/image/logo.png') }}" alt="MonImmo">
			</a>
		</div>

		<!-- Liens & Boutons -->
		<div class="nav">
			<a class="link" href="{{ app.user ? path('app_property_index') : path('app_home') }}">
				<i class="bi bi-house-door-fill me-1"></i>
				Accueil
			</a>

			{% if is_granted('ROLE_USER_ADMIN') %}
				<div class="separator"></div>
				<a class="link" href="{{ path('app_employe_index') }}">
					<i class="bi bi-people-fill me-1"></i>
					Mes employés
				</a>
			{% endif %}

			<div class="separator"></div>

			{% if not app.user %}
				<a class="link" href="{{ path('app_register') }}">Créer une entreprise</a>
				<a class="link" href="{{ path('app_login') }}">Connexion</a>
			{% else %}
				<span class="link text-white">👤
					{{ app.user.email }}</span>
				<a class="link" href="{{ path('app_logout') }}">Déconnexion</a>
			{% endif %}
		</div>
	</div>
</div>
